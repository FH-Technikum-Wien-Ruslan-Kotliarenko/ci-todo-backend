name: Backend CI/CD

on:
  push:
    branches: [ "main", "deploy/production" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'

      - name: Install Dependencies
        run: npm install

      - name: Lint Code
        run: npm run lint

      - name: Run Tests
        run: npm run test
        env: 
          DB_DIALECT: ${{ secrets.DB_DIALECT }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PW: ${{ secrets.DB_PW }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_NAME: ${{ secrets.DB_NAME }}

      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

      - name: Snyk Monitor
        uses: snyk/actions/node@master
        env:
            SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
            command: monitor

      - name: Snyk Test
        uses: snyk/actions/node@master
        continue-on-error: true # ideally should be false, but for demo purposes we set it to true
        env:
            SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
            args: --severity-threshold=high

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build image (amd64)
        run: |
          IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/ci-todo-backend
          TAG=${{ github.sha }}
          docker build --platform linux/amd64 -t $IMAGE:$TAG -t $IMAGE:latest .
          echo "IMAGE=$IMAGE" >> $GITHUB_ENV
          echo "TAG=$TAG" >> $GITHUB_ENV

      - name: Push
        run: |
          docker push $IMAGE:${{ env.TAG }}
          docker push $IMAGE:latest

  deploy-green:
    if: startsWith(github.ref, 'refs/heads/deploy/production')
    needs: build-and-push
    runs-on: ubuntu-latest
      
    steps:
      - name: Upload k6 scripts
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          source: "k6/*"
          target: "~/app/k6"

      - name: SSH deploy to EC2 (green)
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          script: |
            set -Eeuo pipefail
            cd ~/app

            # 1) Update image tag for GREEN
            sed -i '/^BACKEND_GREEN_TAG=/d' .env
            echo "BACKEND_GREEN_TAG=${{ github.sha }}" >> .env

            # 2) Pull & start GREEN
            docker compose pull backend_green
            docker compose up -d backend_green

            # 3) Discover GREEN's published host port for 8080/tcp (e.g., 8083)
            GREEN_PORT="$(docker inspect -f '{{ (index (index .NetworkSettings.Ports "8080/tcp") 0).HostPort }}' backend_green)"
            echo "GREEN_PORT=$GREEN_PORT"

            # 4) Wait until GREEN responds at /health (fast fail if not)
            for i in {1..30}; do
              if curl -fsS --max-time 2 "http://127.0.0.1:${GREEN_PORT}/health" >/dev/null; then
                echo "Green is up on :${GREEN_PORT}"
                break
              fi
              echo "Waiting for green... ($i)"
              sleep 2
              if [ $i -eq 30 ]; then echo "Green failed to start"; exit 1; fi
            done

            # 5) Run k6 smoke (must pass to continue)
            docker run --rm --network host \
              -e BASE_URL="http://127.0.0.1:${GREEN_PORT}" \
              -v "$PWD/k6":/scripts grafana/k6 run /scripts/smoke.js

            # 6) Optional: run CRUD test if API key is provided in .env (kept on EC2)
            #    Add: API_KEY_FOR_CI=<valid key> to ~/app/.env once.
            set +u
            source .env || true
            set -u
            if [ -n "${API_KEY_FOR_CI:-}" ]; then
              echo "Running CRUD k6 with API key"
              docker run --rm --network host \
                -e BASE_URL="http://127.0.0.1:${GREEN_PORT}" \
                -e API_KEY="${API_KEY_FOR_CI}" \
                -v "$PWD/k6":/scripts grafana/k6 run /scripts/crud.js
            else
              echo "API_KEY_FOR_CI not set; skipping CRUD k6"
            fi

            # 7) Flip Nginx to GREEN and restart (only if all k6 exited 0)
            sed -i 's/set \$active_backend backend_blue;/set \$active_backend backend_green;/' nginx.conf
            docker compose restart nginx

  notify:
    if: failure()
    needs: [build-and-push, deploy-green]
    runs-on: ubuntu-latest

    steps:
      - name: Post text to a Slack channel
        uses: slackapi/slack-github-action@v2.0.0
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            "channel": "${{ secrets.SLACK_BACKEND_CHANNEL_ID }}",
            "text": "🚨 Backend CI pipeline failed for commit ${{ github.sha }}"